var marked = <%= @thing.marked %>;
var element = $('.<%= @thing.id %>_marked i');
if(marked)
  element.addClass('marked');
else
  element.removeClass('marked');
$('.d-marked-count').html('<%= @marked_count %>');
if(marked) {
  window.raj_mt_loader.triggerLoad();
}